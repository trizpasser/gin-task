name: Deploy to GKE

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Configure Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v0.5.0
      with:
        version: '358.0.0'
        service_account_key: ${{ secrets.GINSECRET }}

    - name: Configure kubectl and set namespace
      run: |
        gcloud container clusters get-credentials cluster-gin --zone us-central1-a   # Substitua pelos dados do seu cluster GKE
        kubectl config set-context --current --namespace=gin-space  # Substitua pelo nome do namespace específico

    - name: Deploy to GKE
      run: |
        kubectl apply -f deployment.yaml
        kubectl apply -f service.yaml
